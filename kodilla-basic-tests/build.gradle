plugins {
    id("java")
}

group = "com.kodilla"
version = "1.0-SNAPSHOT"

tasks.test {
useJUnitPlatform()
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation group: "org.junit.jupiter", name: 'junit-jupiter-engine', version: '5.10.5'
}